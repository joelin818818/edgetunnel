# 2026-07-31 工作日志

## 项目：edgetunnel (joelin818818/edgetunnel) Cloudflare Worker 代理脚本
- 仓库已克隆到 `d:/Personal/szjm/Documents/cursor/edge`，远程 `origin` 指向 joelin818818/edgetunnel。
- 主脚本 `_worker.js`（6451 行）：反向隧道，支持 VLESS/Trojan/SS/gRPC/XHTTP/WS，订阅生成，管理后台。

## 完成：新建 `_worker_nokv.js`（取消 KV 持久化版本）
用户要求取消 KV 持久化，改为文件头配置常量。已确认：
- 仅保留「本地随机IP库」生成订阅 IP（取消 ADD.txt / 远程优选订阅生成器）
- 取消：cf.json、tg.json、ADD.txt、log.json、配置保存/重置(KV)、代理连通性检测、CF 用量查询、TG 通知

### 具体改动（_worker_nokv.js）
1. 文件头新增 `USER_CONFIG` 常量区块（替代 config.json），注释说明。
2. 后台 admin 路由：移除 KV/TG/CF/ADD/代理检测/日志分支，仅保留只读查看（config.json 返回、ADD.txt 返回随机IP、cf.json 返回 request.cf）。
3. 订阅块：移除远程优选生成器调用（`获取优选订阅生成器数据`/`请求优选API`），仅本地随机IP库。
4. `读取config_JSON`：改为 `USER_CONFIG` 深度合并到内置默认配置，不再 `env.KV.get/put`。
5. 删除函数：`请求日志记录`、`掩码敏感信息`、`getCloudflareUsage`、`获取优选订阅生成器数据`、`请求优选API`。
6. TG/CF 固定为禁用空对象。
- `node --check _worker_nokv.js` 通过。

## 待办
- 用户未明确是否要 commit/push（原话"修改了也要同步上去"），需在完成后提交并 push 到 origin。
